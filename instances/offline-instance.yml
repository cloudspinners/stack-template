---
stack_instance:

  stack_name: example-stack
  instance_id: offline-test

  platform:
    name: localstack
    parameters:
      aws_region: us-east-1
      access_key: test
      secret_key: test

  tool: 
    name: terraform
    backend:
      name: local
