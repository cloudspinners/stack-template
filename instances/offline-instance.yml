---
stack_instance:

  stack_name: example-stack
  instance_id: offline-test

  platform:
    name: aws
    parameters:
      aws_region: us-east-2
      aws_profile: spintools_aws

  tool: 
    name: terraform
    backend:
      name: local
