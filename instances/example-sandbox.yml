---
stack_instance:

  stack_name: network-stack
  instance_id: kief_sandbox

  platform:
    name: aws
    parameters:
      aws_region: us-east-2
      aws_profile: spintools_aws

  tool: 
    name: terraform
    backend:
      name: local
